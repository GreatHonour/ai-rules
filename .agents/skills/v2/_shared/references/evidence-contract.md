# 证据契约

每条证据至少包含：交付 ID、用例 ID、观察结果、环境、源码版本、捕获时间和仓库相对文件路径。截图只在显示相关状态时有价值；确定性检查优先保留机器可读日志。

验收映射固定为：

`document → requirement → case → result → evidence`

通过前必须检查：

1. 每份登记文档都有需求映射。
2. 每条需求都有可执行用例。
3. 每个通过或失败用例都有真实证据。
4. 阻塞和跳过都有具体原因。
5. 证据来自当前运行、当前提交，且已脱敏。

证据不得包含凭证、个人数据、私有 URL、绝对路径、客户原始标识或无关日志。
