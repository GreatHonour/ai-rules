# 拆分用户说明与项目说明

## 目标

- 类型：小型变更
- 目标：访问 `README.md` → 仅看到安装、初始化、日常命令及用户需要了解的行为约束；访问 `PROJECT.md` → 看到公共资源维护、开发验证、发布入口和仓库结构说明
- 边界：不修改 CLI 行为、命令参数、包配置或发布流程；保留 `README.md` 当前已暂存的删除内容，不覆盖其他工作区改动

## 历史依据

- 功能文档：现有 `README.md` 同时包含用户操作与仓库维护说明；`RELEASE.md` 已独立记录 Changesets 与 npm 发布流程
- 代码历史：`511db38` 首次加入 CLI 说明，`fb6c147` 加入工程工具链说明，`d02df68` 更新发布信息；本次仅调整文档职责，不改变历史约定

## 实现

- 精简 `README.md` → 保留环境要求、安装初始化、`config`、`update`、`upload` 及面向用户的安全语义
- 新增 `PROJECT.md` → 收纳 registry 维护、开发与质量检查、发布命令入口、目录职责
- 在 `README.md` 增加项目文档入口，在 `PROJECT.md` 增加用户文档入口 → 建立双向导航并避免内容重复

## 验证场景

- 从零使用 CLI → 只阅读 `README.md` 即可完成安装、初始化和日常操作
- 维护仓库或公共资源 → 从 `README.md` 进入 `PROJECT.md`，可找到维护、验证、发布命令和目录信息
- 检查文档重复 → 同一维护流程只在一处完整说明，链接目标存在且命令与 `package.json` 一致

## 影响范围

- `README.md`
- 新增 `PROJECT.md`
- 新增本变更记录；不纳入其他已修改文件
