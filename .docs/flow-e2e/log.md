## 功能：flow-e2e

### 2026-09-17 任务状态与证据归档

- 能力或变更：按来源任务依赖执行 E2E 验证，将通过、失败和阻塞结果的任务专属证据地址统一保存在 `evidence` 数组。
- 关键逻辑：恢复执行时按来源任务 ID 重建状态快照；同一次验证可复用给多个任务，但迭代级结果只记录一次，不复制为任务专属证据。
- 结构决策：移除 `error_log`，保留旧状态读取迁移；新证据必须是当前迭代 `evidence/` 中存在的文件。
