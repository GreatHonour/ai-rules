<!-- 1. **每次输出后必须跟一段"自我挑战"**：挑战本次输出是否有思维遗漏，尝试找到第一性原理，并根据挑战补充回答，直到答案完整
2. **每次都要犀利审视用户输入**：主动找出潜在问题，给出用户思考框架之外的建议；用户说的太离谱就直接指出，帮助其清醒
3. **给出答案之前**：请先告诉我还缺少哪些关键信息，以及这些信息可能如何改变你的答案，并且人们在处理这类问题时最常犯的一个错误
4. **请只向我提出一个最关键的问题**：这个问题应该能够帮助你理解我的真实目标和具体情况，让最终答案真正对我有用，而不是一份谁都能套用的通用建议
5. **函数使用 `jsdoc` 中文注释，变量使用普通注释**
6. 包管理使用 `pnpm`，使用中文回复 -->

# flow-* 具体要改什么

## flow-brainstorm

保留原流程，增加：

- 需求编号；
- 成功标准的可观察性；
- 每个成功标准对应的验证方式；
- 外部依赖和授权边界；
- 哪些结论必须等实现后才能确认。
  建议增加：

### 可验证目标

| 目标 | 观察方式 | 证据类型 | 验证阶段 |
| ---- | -------- | -------- | -------- |

## flow-plans

保留 design.md 和 task.md，增加：

- 稳定的决策编号；
- 功能点编号；
- 任务编号；
- 验证任务编号；
- 风险与证据要求。

## flow-plan-review

这是最适合吸收 DeliveryGuard 的位置。
新增检查：

- 每个业务决策是否有设计映射；
- 每个功能点是否有任务映射；
- 每个任务是否有验证方式；
- 每个验证是否有证据定义；
- 是否存在只能“口头证明”的任务；
- 是否把环境阻塞误写成代码失败；
- 是否遗漏回滚、清理、幂等和副作用控制。

## flow-implement

保留原有依赖调度，但增加：

- 开始前记录基线 commit；
- 每个任务记录实际修改范围；
- 每个任务完成后记录验证命令和结果；
- 任务只能在代码和验证都完成后标记 [x]；
- 测试基础设施失败不能标记为代码修复成功；
- 跨仓库依赖无法验证时标记 blocked。

## flow-review

保留“先报告、后确认、再修复”的交互。
增加：

- 审查基准 commit；
- 证据完整性检查；
- 结论级别检查；
- 是否存在把测试成功扩大成版本成功；
- 是否存在未声明的外部副作用；
- 是否有 blocked/skipped 被伪装成 passed。

## flow-e2e

这是需要改动最多的 skill。
增加：

- 执行前能力预检；
- 分层结果；
- 每个场景独立证据；
- 失败和阻塞分开；
- 运行前后环境锚点；
- 账号、设备、数据和部署信息的脱敏；
- 清理动作及清理证据。
  建议把报告改成：

### 场景 V1

- 结果：passed / failed / blocked / skipped / inconclusive
- 环境：
- 版本：
- 执行时间：
- 观察结果：
- 证据：
- 未验证层：
- 下一步：

## flow-bugfix

吸收：

- 可复现 baseline；
- 失败测试；
- 修复候选 commit；
- 回归测试；
- 第一失败边界；
- 根因置信度；
- 配置/数据/依赖问题与代码问题区分。
  可以把现有 fix.md 扩展为 Repair Case，但不必直接改名。

## flow-archive

保留原有归档动作，但增加归档前门禁：

- 所有任务都有最终状态；
- 所有验证场景都有结论；
- 通过结论都有证据；
- blocked/skipped 都有原因；
- review 问题都有状态；
- 必要的 E2E 已完成；
- 版本、commit、部署事实没有臆造；
- 归档不能自动推导发布成功。
  同时建议把 git commit 从“默认动作”改成：
  在归档检查通过后执行，并将 commit hash 写入归档记录。

## 原流程是否需要改变

建议是：
主流程不变，增加证据门禁和状态语义
保留：
新功能：

```text
brainstorm
→ plans
→ plan-review
→ implement
→ review
→ e2e
→ archive
```

```text
Bug：
bugfix
→ implement
→ review
→ e2e（如有必要）
→ archive
```

增加的是每个阶段的“交付契约”：

```text
阶段输入
→ 阶段动作
→ 阶段产物
→ 验证证据
→ 状态结论
→ 是否允许流转
```

不建议直接把 flow 改造成 DeliveryGuard 的 20 个 skill，因为这会带来三个问题：

1. 简单任务的流程成本显著增加；
2. 需求探索和工程治理混在一起；
3. skill 名称和路由复杂度上升，用户难以判断该调用哪个 skill。

# 第二阶段：增加共享模板

各 flow skill 引用这些规则，避免每个 skill 自己维护一份不一致的状态定义。
新增一个共享模板目录，例如：

```text
D:\skills\.agents\skills\flow-shared\
  evidence-contract.md
  status-model.md
  traceability-matrix.md
  completion-gate.md
```

# 第三阶段：增加轻量校验脚本

建议做一个本地只读校验器，检查：

- design.md 中的功能点是否都出现在 task.md；
- 任务是否有验证方式；
- 通过任务是否有证据路径；
- 集成验证是否有结果；
- blocked/skipped 是否有原因；
- archive 前是否仍有未完成项。
  不要一开始就引入完整 DeliveryGuard 服务。先让 Markdown 契约稳定，再决定是否需要结构化数据库或 CLI。
